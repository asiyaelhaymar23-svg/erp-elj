# Copier ce fichier en .env pour le développement local (hors Docker).
# Vite lit cette variable au build ; en conteneur, l'URL réelle est
# injectée au démarrage (voir docker-entrypoint.d/40-env-config.sh).
VITE_API_URL=http://localhost:3000
